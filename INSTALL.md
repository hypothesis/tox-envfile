# Installing tox-envfile

Just add `tox-envfile` to the `requires` setting in the `[tox]` section of your `tox.ini` file:

```ini
[tox]
requires =
  tox-envfile
```

See [`requires` in tox's docs](https://tox.wiki/en/latest/config.html#conf-requires).
